export class Member {
      name: string;
      completed: Boolean;
      turn: Boolean;

      constructor(name: string) {
            this.name = name;
            this.completed = false;
      }
}
